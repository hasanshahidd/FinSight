# Backend (used for dashboard data: transactions, insights, users)
VITE_BACKEND_URL=http://localhost:8000

# n8n webhook — ALL chat messages route through this URL.
# n8n is the only chat path. There is no direct-to-backend fallback.
VITE_N8N_WEBHOOK_URL=http://localhost:5678/webhook/finance-chat
